package com.spicejet.pages;

public class HerokuPage {
/*
    @FindBy(tagName = "h1")
    private WebElement welcomePageTitle;

    public HerokuPage(WebDriver driver) {
        super(driver);
    }


    public void getHerokuPage() {
        try {
            driver.get(PropertyReader.getProperty("url"));
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    public String getWelcomePageTitle(){return welcomePageTitle.getText();}

 */
}
