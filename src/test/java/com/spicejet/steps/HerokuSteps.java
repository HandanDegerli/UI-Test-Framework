package com.spicejet.steps;

import com.spicejet.pages.HerokuPage;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;

import static org.junit.Assert.assertEquals;

public class HerokuSteps{
/*
    private final HerokuPage herokuPage;

    public HerokuSteps() {
        this.herokuPage = new HerokuPage(driver);
    }

    @Given("User is getting Heroku Page")
    public void userIsGettingHerokuPage() {
        herokuPage.getHerokuPage();
    }
    @Then("User should see the welcome page title as {string} on Heroku Page")
    public void userShouldSeeTheWelcomePageTitleAsOnHerokuPage(String welcomePageTitle) {
        assertEquals(herokuPage.getWelcomePageTitle(), welcomePageTitle);
    }

 */
}
